<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>comment: {{comments.text}} by {{comments.user.name}}</p>

  <p>
    <button class="btn btn-primary">Fund this Project!</button>
  </p>
</div>
